function addBorder(){
    const img = document.getElementById("border")

    if (img.classList.contains  ("borde")){
        img.classList.remove ("borde")
    } else {
        img.classList.add("borde")
    }
}
